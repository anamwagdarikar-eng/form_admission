# FY Engineering Admission Form

Streamlit implementation of the supplied first-year engineering admission form, with English labels and Marathi subtitles.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```

The supplied paper form is shown in the sidebar as a visual reference.